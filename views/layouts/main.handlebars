{{! Default layout for all handlebars files rendered, which is inherited by all other templates. }}

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>{{title}}</title>
        <link rel="stylesheet" href="/public/main.css" />
    </head>

    <body>
        {{!-- navigation bar for all tabs --}}
        <header>
            <div class="bar">
                <h1 class="title-text">Book.it Scheduler</h1>
                <nav>
                    <ul>
                        <li><a href="/">About</a></li>
                        {{#if loggedIn}}
                        <li><a href="/meetings">My Meetings</a></li>
                        <li><a href="/profile">Profile</a></li>
                        <li><a href="/signout">Sign Out</a></li>
                        {{else}}
                        <li><a href="/login">Log In</a></li>
                        <li><a href="/signup">Sign Up</a></li>
                        {{/if}}
                    </ul>
                </nav>
            </div>
        </header>
        {{{body}}}
    </body>
</html>
